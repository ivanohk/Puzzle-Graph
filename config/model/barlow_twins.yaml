name: barlow_twins

encoder:
  name: gin
  hidden_dim: 256
  num_layers: 3
  pool: false

proj_dim: 256
lambda_param: null

augment:
  - {name: edge_drop, p: 0.2}

optimizer:
  lr: 3.0e-4
  weight_decay: 1.0e-5
  warmup_epochs: 20

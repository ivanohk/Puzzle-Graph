name: graphcl

encoder:
  name: gin
  hidden_dim: 256
  num_layers: 3
  pool: false

proj_dim: 256
tau: 0.5

augment:
  - {name: edge_drop, p: 0.2}
  - {name: feat_mask, p: 0.2}

optimizer:
  lr: 1.0e-3
  weight_decay: 1.0e-5
